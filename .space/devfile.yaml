schemaVersion: 2.2.0
attributes:
  space:
    instanceType: regular
    editor:
      type: Fleet
components:
- name: image-build
  image:
    imageName: space-web:latest
    dockerfile:
      buildContext: .space
      uri: .space/Dockerfile
