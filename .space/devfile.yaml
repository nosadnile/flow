schemaVersion: 2.2.0
attributes:
  space:
    instanceType: regular
    editor:
      type: Idea
components:
- name: image-build
  image:
    imageName: space-web:latest
    dockerfile:
      buildContext: .space
      uri: .space/Dockerfile
